# Optional configuration. Copy to .env only for local tooling; do not commit it.
#
# DeepSeek is only needed to regenerate the natural-language memory descriptions.
# DEEPSEEK_API_KEY=
#
# Default: http://localhost:3001/sparql
# MEMQ_SPARQL_ENDPOINT=
